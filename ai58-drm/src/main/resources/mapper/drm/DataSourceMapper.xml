<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai58.drm.mapper.DataSourceMapper">

    <resultMap type="DataSource" id="DataSourceResult">
        <id     property="sourceId"      column="source_id"      />
        <result property="createBy"        column="create_by"        />
        <result property="createTime"      column="create_time"      />
        <result property="updateBy"        column="update_by"        />
        <result property="updateTime"      column="update_time"      />
        <result property="remark"          column="remark"           />
        <result property="sourceName" column="source_name" />
        <result property="sourceType" column="source_type" />
        <result property="connectionUrl" column="connection_url" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="status" column="status" />
    </resultMap>

    <sql id="selectDataSourceVo">
        select source_id, source_name, source_type, connection_url, username, password, status, create_by, create_time, update_by, update_time, remark
        from drm_data_source
    </sql>

    <select id="selectDataSource" parameterType="DataSource" resultMap="DataSourceResult">
        <include refid="selectDataSourceVo"/>
        <where>
            <if test="sourceId != null">
                and source_id = #{screenId}
            </if>
        </where>
    </select>

    <select id="selectDataSourceList" parameterType="DataSource" resultMap="DataSourceResult">
        <include refid="selectDataSourceVo"/>
        <where>
            <if test="params.beginTime != null and params.beginTime != ''">
                and date_format(create_time,'%y%m%d') &gt;= date_format(#{params.beginTime},'%y%m%d')
            </if>
            <if test="params.endTime != null and params.endTime != ''">
                and date_format(create_time,'%y%m%d') &lt;= date_format(#{params.endTime},'%y%m%d')
            </if>
        </where>
    </select>

    <insert id="insertDataSource" parameterType="DataSource">
        insert into drm_data_source (
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>

    <update id="updateDataSource" parameterType="DataSource">
        update drm_data_source
        <set>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        where source_id = #{sourceId}
    </update>

    <delete id="deleteDataSourceById" parameterType="Long">
        delete from drm_data_source where source_id = #{sourceId}
    </delete>

    <delete id="deleteDataSourceByIds" parameterType="Long">
        delete from drm_data_source where source_id in
        <foreach item="sourceId" collection="array" open="(" separator="," close=")">
            #{screenId}
        </foreach>
    </delete>

</mapper>